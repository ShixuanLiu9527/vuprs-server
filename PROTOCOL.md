# VUPRS Network Protocol V1.0
# VUPRS 网络通信协议 V1.0

## 协议概述

系统网络通信协议的基于 `TCP` (用于控制和应答) 和 `UDP` (用于数据传输).  
对于每一个会话, 系统网络通信协议都被抽象为 `4` 个不同功能的通道, 这些通道均使用同一物理层.  
每个消息由 `二进制消息头` 和 `二进制消息体数据包` 组成. 在发送时, `二进制消息头` 发送完成后会紧挨着发送 `二进制消息体数据包` (如果有).  
**需要说明的是, 本文档并没有约定协议中的二进制取值, 协议每个信号的取值约定可以通过相关 `JSON` 配置文件调整**.  

## Part 1 - 逻辑通道与消息头

通道之间通过消息头区分, 所有通道的消息头都具有固定的字节大小, 由以下紧凑结构体定义:  

    #pragma pack(push, 1)
    struct Header
    {
        uint32_t magic;

        uint8_t version;
        uint8_t channel;
        uint8_t msg_type;
        uint8_t response_flag;

        uint32_t msg_id;
        uint32_t msg_response_id;

        uint32_t body_size;

        uint32_t udp_stream_id;
        uint32_t udp_packet_sequence;
        uint32_t udp_total_packet_count;
    };
    #pragma pack(pop)

### 1. 魔数: `magic`

`magic` 是协议的魔数, 表示消息的开始, 进程通过捕捉 `magic` 了解何时接收数据.  

### 2. 协议版本: `version`

`version` 是协议版本.

### 3. 通道描述: `channel`

不同通道通过 `channel` 中的信息区分, 目前通道的可选项有:  
  
_表1 协议通道描述_
| 通道名称 | 描述 | 通信协议 | 说明 |
| :--- | :--- | :--- | :--- |
| `CHANNEL__CONTROL` | `控制通道` | `TCP` | 在这个通道里完成主机对板子的控制参数设置, 该通道的指令必须在本通道被应答, 否则上位机将持续发送控制指令. |
| `CHANNEL__NOTIFY` | `通知通道` | `TCP` | 用于报告主机或板子的当前状态. |
| `CHANNEL__DATA` | `数据通道` | `UDP` | 板子到主机的数据通过该通道传输, 该通道传输不需要应答. |
| `CHANNEL__HEARTBEAT` | `心跳通道` | `TCP` | 主机和板子通过该通道探测两边设备的连接状态, 该通道具有最低优先级, 进程可以先处理其他通道的重要数据. |

### 4. 消息类型: `msg_type`

`msg_type` 是消息类型, 不同通道应该具有不同的消息类型, 通道间的逻辑是完全独立的.  

### 5. 回复标志: `response_flag`

回复标志表示本消息是否需要下位机回复, 有以下 `3` 种取值:  
  
_表2 回复标志_
| 类型名称 | 描述 |
| :--- | :--- |
| `RSP__NONE` | 不需要下位机回复 |
| `RSP__NORMAL` | 常规消息, 需要下位机回复 |
| `RSP__EMERGENCY` | 紧急消息, 需要下位机**立即**回复 |

### 6. 消息 `ID`: `msg_id`

`msg_id` 当前消息的 `ID`, 各个通道间的消息不互通, `ID` 是独立的.  

### 7. 响应 `ID`: `msg_response_id`

`msg_response_id` 是当前消息响应的父消息的 `ID`, 各通道间是独立的.  

### 8. 消息体大小: `body_size`

`body_size` 用于定义消息体大小, 进程先接收消息头, 然后再接收消息体 (如果 `body_size` 不为 `0`), 可以使用 `MSG_WAITALL` 方法读取指定字节数.  

### 9. `UDP` 流 `ID`: `udp_stream_id`

[**只在 `数据通道` 有效**, 其他通道恒为 `0`] 用于表示 `UDP` 数据流编号, 下位机将同一编号的所有 `UDP` 数据包合成为一个数据包.  

### 9. `UDP` 包顺序: `udp_packet_sequence`

[**只在 `数据通道` 有效**, 其他通道恒为 `0`] 用于表示当前 `UDP` 数据包在当前传输的数据流中的顺序, 下位机按照此顺序将 `UDP` 数据包合成为一个数据包.  

### 10. `UDP` 总包数: `udp_total_packet_count`  

[**只在 `数据通道` 有效**, 其他通道恒为 `0`] 用于表示当前 `UDP` 数据流中包含数据包的个数, 下位机收到该数量的数据包后即认为收到了一个完整的数据流, 并开始打包工作.  

## Part 2 - 消息类型和消息体数据包

协议中的 `4` 个通道具有不同的消息类型.

### 控制通道

控制通道的消息类型有 `设置类型`, `查询类型` 和 `响应类型`.  
其中 `设置类型` 和 `查询类型` 是主机下发给板子的指令, `响应类型` 是板子回复主机的操作结果.  
  
**TYPE 1**: `设置类型`  
`设置类型` 的类型名称和对应消息体数据内容有 (下表中 `-` 表示没有附加的消息体数据):  
  
_表3 控制通道消息类型 - 设置类型_  
| 类型名称 | 描述 | 消息体数据包内容 | 回复标志 |
| :--- | :--- | :--- | :--- |
| `CONTROL__MSG__START_SAMPLING` | 开启采样 (下位机开始采样和相关数据计算, 数据通道也会使用 `UDP` 协议自动发送采样数据) | - | `RSP__NORMAL` |
| `CONTROL__MSG__STOP_SAMPLING` | 采样停止 (下位机停止采样和相关数据计算, 数据通道也会停止发送采样数据) | - | `RSP__NORMAL` |
| `CONTROL__MSG__DISCONNECT` | 电脑端断开连接 (下位机复位到初始状态, 关闭全部采样和输出处理计算) | - | `RSP__NORMAL` |
| `CONTROL__MSG__RESET` | 下位机复位 | - | `RSP__NORMAL` |
| `CONTROL__MSG__SET_SAMPLING_FREQUENCY` | 设置采样频率 | `uint16_t`, 代表单位为 `Hz` 的采样频率 | `RSP__NORMAL` |
| `CONTROL__MSG__SET_DATA_PACKAGE_SIZE` | 设置数据包大小 (下位机修改发送到上位机的数据包大小) | `uint32_t`, 代表数据通道的消息体数据包字节大小 | `RSP__NORMAL` |
| `CONTROL__MSG__SET_DATA_TYPE` | 设置数据包类型 (下位机修改发送到上位机的数据包大小) | `uint8_t`, 数据包类型代号 ``CONTROL__BODY_DATA__DATA_TYPE__**` | `RSP__NORMAL` |
  
其中 `CONTROL__MSG__SET_DATA_TYPE` 对应的数据类型可以有以下值:  
  
1. `CONTROL__BODY_DATA__DATA_TYPE__RAW_SIGNAL`  
各个通道的原始采样数据 (这是多通道数据, 通道数量取决于阵列配置文件中的阵列定义). **当设置该值时, 板子仅进行采样, 停止其他计算**.  
2. `CONTROL__BODY_DATA__DATA_TYPE__TARGET_SIGNAL`  
目标位置的波束形成结果数据 (这是单通道数据, 由多通道波束形成器计算得到). **当设置该值时, 板子同时进行采样和单指向波束形成计算, 停止其他计算**.  
3. `CONTROL__BODY_DATA__DATA_TYPE__SCAN_POWER`  
波束形成扫描数据 (这是二维数据, 每个点表示不同位置的波束形成扫描结果信号的功率). **当设置该值时, 板子同时进行采样和多指向波束形成计算, 停止其他计算**.  
4. `CONTROL__BODY_DATA__DATA_TYPE__SCAN_PROBABILITY_*`  
波束形成扫描故障概率 (这是二维数据, 每个点表示不同位置的波束形成扫描结果信号的故障概率). **当设置该值时, 板子同时进行采样, 多指向波束形成和故障识别计算**, 符号 `*` 表示不同故障类型, 需要在后续开发中确定.  
  
**TYPE 2**: `查询类型`  
`查询类型` 的类型名称和对应消息体数据形式有 (下表中 `-` 表示没有附加的消息体数据):  
  
_表4 控制通道消息类型 - 查询类型_  
| 类型名称 | 描述 | 消息体数据类型 | 回复标志 |
| :--- | :--- | :--- | :--- |
| `CONTROL__MSG__QUERY_SAMPLING_FREQUENCY` | 查询采样频率 | - | `RSP__NORMAL` |
| `CONTROL__MSG__QUERY_DATA_TYPE` | 查询数据类型 | - | `RSP__NORMAL` |
| `CONTROL__MSG__QUERY_DEVICE_ID` | 查询设备 `ID` 号 | - | `RSP__NORMAL` |

**TYPE 3**: `响应类型`  
`响应类型` 的类型名称和对应消息体数据形式有 (下表中 `-` 表示没有附加的消息体数据):  
  
_表5 控制通道消息类型 - 响应类型_  
| 类型名称 | 描述 | 消息体数据类型 | 回复标志 |
| :--- | :--- | :--- | :--- |
| `CONTROL__MSG__SUCCESS` | 操作成功, 用于回复 `设置类型` 的消息 | - | `RSP__NONE` |
| `CONTROL__MSG__FAILED` | 操作失败, 用于回复 `设置类型` 的消息 | `uint8_t`, 错误类型代号 `CONTROL__BODY_DATA__ERROR__**` | `RSP__NONE` |
| `CONTROL__MSG__DISCONNECT_READY` | 准备断开就绪 | - | `RSP__NONE` |
| `CONTROL__MSG__SAMPLING_FREQUENCY` | 采样频率, 用于回复 `查询类型` 的消息 | `uint16_t`, 代表单位为 `Hz` 的采样频率 | `RSP__NONE` |
| `CONTROL__MSG__DATA_TYPE` | 采样频率, 用于回复 `查询类型` 的消息 | `uint8_t`, 数据包类型代号 `CONTROL__BODY_DATA__DATA_TYPE__**` | `RSP__NONE` |

其中 `CONTROL__BODY_DATA__ERROR__**` 的数据类型代号需要后续开发确定.  

### 消息通道

消息通道的消息类型有 `查询类型` 和 `响应类型`. 主要用于板子给上位机传递当前状态.  

**TYPE 1**: `查询类型`  
`查询类型` 的类型名称和对应消息体数据形式有 (下表中 `-` 表示没有附加的消息体数据):  
  
_表6 消息通道消息类型 - 查询类型_  
| 类型名称 | 描述 | 消息体数据类型 | 回复标志 |
| :--- | :--- | :--- | :--- |
| `NOTIFY__MSG__QUERY_CPU_TEMPERATURE` | 板子 `CPU` 温度 | - | `RSP__NORMAL` |
| `NOTIFY__MSG__QUERY_STORAGE` | 板子剩余存储空间 | - | `RSP__NORMAL` |
| `NOTIFY__MSG__QUERY_BATTERY_LEVEL` | 电池电量 | - | `RSP__NORMAL` |

**TYPE 2**: `响应类型`  
`响应类型` 的类型名称和对应消息体数据形式有 (下表中 `-` 表示没有附加的消息体数据):  
  
_表7 消息通道消息类型 - 响应类型_  
| 类型名称 | 描述 | 消息体数据类型 | 回复标志 |
| :--- | :--- | :--- | :--- |
| `NOTIFY__MSG__CPU_TEMPERATURE` | 板子 `CPU` 温度, 用于回复 `NOTIFY__MSG__QUERY_CPU_TEMPERATURE` 消息 | `uint16_t`, `float` 类型的温度 (单位: 摄氏度), 传输时转换为 `uint16_t` | `RSP__NORMAL` |
| `NOTIFY__MSG__STORAGE` | 板子剩余存储空间, 用于回复 `NOTIFY__MSG__QUERY_STORAGE` 消息 | `uint32_t`, 设备存储空间 (单位: `MB`) | `RSP__NORMAL` |
| `NOTIFY__MSG__BATTERY_LEVEL` | 电池电量, 用于回复 `NOTIFY__MSG__QUERY_BATTERY_LEVEL` 消息 | `uint16_t`, `float` 类型的电量 (单位: `%`), 传输时转换为 `uint16_t` | `RSP__NORMAL` |

### 数据通道

控制通道的消息类型只有 `数据类型`.  
`数据类型` 的类型名称和对应消息体数据形式有 (下表中 `-` 表示没有附加的消息体数据):  
  
_表8 数据通道消息类型 - 数据类型_  
| 类型名称 | 描述 | 消息体数据类型 | 回复标志 |
| :--- | :--- | :--- | :--- |
| `DATA__MSG__RAW_SIGNAL` | 各个通道的原始采样数据 | 多字节数据 | `RSP__NONE` |
| `DATA__MSG__TARGET_SIGNAL` | 目标位置的波束形成结果数据 | 多字节数据 | `RSP__NONE` |
| `DATA__MSG__SCAN_POWER` | 波束形成扫描功率数据 | 多字节数据 | `RSP__NONE` |
| `DATA__MSG__SCAN_PROBABILITY_*` | 波束形成扫描故障概率数据 | 多字节数据 | `RSP__NONE` |

### 心跳通道
  
心跳通道的消息类型有 `紧急询问类型` 和 `连接\响应类型`.  
其中 `连接\响应类型` 共有一个消息, 当上位机下发紧急询问类型时, 如果下位机没有断连, 则回复一个 `连接\响应类型` 的消息.  
当上位机没有要求下位机回复时, 下位机定时发送一次 `连接\响应类型` 的消息.  
  
**TYPE 1**: `紧急询问类型`  
`紧急询问类型` 的类型名称和对应消息体数据形式如下 (下表中 `-` 表示没有附加的消息体数据):
  
_表9 心跳通道 - 连接类型_  
| 类型名称 | 描述 | 消息体数据类型 | 回复标志 |
| :--- | :--- | :--- | :--- |
| `HEARTBEAT__MSG__EMERGENCY_ACK` | 是否设备在线 | - | `RSP__EMERGENCY` |

**TYPE 2**: `连接\响应类型`  
`连接\响应类型` 的类型名称和对应消息体数据形式如下 (下表中 `-` 表示没有附加的消息体数据):
  
_表10 心跳通道 - 连接\响应类型_  
| 类型名称 | 描述 | 消息体数据类型 | 回复标志 |
| :--- | :--- | :--- | :--- |
| `HEARTBEAT__MSG__IS_ONLINE` | 是否设备在线 | - | `RSP__NONE` |

当心跳通道长时间 (距离上一次刷新超过一定时间后) 没有状态刷新时, 上位机或下位机会在 `心跳通道` 发送一个紧急报文, 如果目标设备在设置的超时时间内仍然没有回复, 则认为设备断连.  
发生断连时, 板端服务器会关闭当前会话, 设备端会提示设备离线.  

## Part 3 - 通信交互流程
  
系统连接后应按照以下步骤进行数据传输.  
  
### Step-1 建立连接 
  
系统建立连接后, 电脑端询问服务器的设备 `ID`, 确认是否为合法设备 `ID`, 确认完成后开始下一步. 此时 `心跳通道` 开始相互定时发送连接数据包.  
如果是非法 `ID`, 电脑端应该输出一些提示信息.  
  
### Step-2 电脑端操作服务器端采集数据
  
在校验完成后, 初始时, 系统应处于静默状态, 此时板子没有采样, 数据通道没有数据, 仅 `心跳通道` 有数据包互通.  
电脑端通过 `控制通道` 下发开始采样消息, 板子服务器收到消息后开启采样和计算, 并对开启状态进行回复, 如果出现错误应在消息体数据包中显示错误类型.  
在采样开始后, 电脑端才可以下发指令控制其他参数或数据类型.  
  
### Step-3 断开设备连接

用户在准备断开时, 电脑可以通过 `控制通道` 下发断开连接指令, 板子服务器复位全部参数, 关闭采样.  
在板子完成复位后, 回复电脑设置成功, 此时电脑就可以退出连接.  

如果直接关闭上位机, 板子服务器通过心跳通道检测到断开连接后也会对参数和采样进行复位.  
